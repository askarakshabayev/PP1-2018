#include <iostream>
#include <cmath>

using namespace std;

int main() {
    // int a, b;
    // cin >> a >> b;    
    // cout << pow(a, b); // a ^ b

    double a = 2.4, b = 2.6;
    // cout << round(a);
    // cout << round(b);

    cout << ceil(a);
    cout << floor(b);
    return 0;
}