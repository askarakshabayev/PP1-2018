// a, b => a + b
#include <iostream>

using namespace std;

int main() {
    freopen("a.in", "r", stdin);
    freopen("a.out", "w", stdout);
    int a, b;
    cin >> a >> b;
    cout << a + b;
    return 0;
}